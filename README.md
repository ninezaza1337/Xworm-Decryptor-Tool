# 🔓 XWorm AES Configuration Decryptor

เครื่องมือถอดรหัสการตั้งค่าของ XWorm RAT ด้วยอัลกอริทึม AES-CBC

## 📋 คำอธิบาย

`aes.py` เป็นเครื่องมือ Python สำหรับถอดรหัสการตั้งค่าที่เข้ารหัสของ XWorm RAT ซึ่งใช้การเข้ารหัส AES-256-CBC พร้อมกับ Base64 encoding

### 🎯 วัตถุประสงค์
- ถอดรหัสการตั้งค่าที่เข้ารหัสของ XWorm RAT
- วิเคราะห์ C&C Server และพารามิเตอร์การเชื่อมต่อ
- สนับสนุนการวิจัยและการศึกษาด้านความปลอดภัยไซเบอร์

## 🛠️ คุณสมบัติ

### 🔑 **Key Derivation System**
- สร้าง AES-256 key จาก Mutex string ด้วย MD5 hashing
- ใช้ Zero-filled IV (16 bytes) สำหรับ CBC mode
- รองรับการสร้าง key แบบ overlapping pattern

### 🔓 **AES Decryption Engine**
- ถอดรหัส Base64 encoded ciphertext
- ใช้ AES-CBC mode decryption
- จัดการ PKCS7 padding และ null-byte termination

### 📊 **Configuration Parser**
- แปลงข้อมูลที่ถอดรหัสเป็น readable text
- ตรวจสอบและระบุประเภทข้อมูล (IP address, Port number)
- รองรับ multiple encoding formats (UTF-8, Latin-1)

<img width="1299" height="792" alt="Screenshot 2025-11-01 183559" src="https://github.com/user-attachments/assets/2b5657ae-f4ca-4444-ab39-a1e7a8c7f5cd" />

#### การตั้งค่าที่สามารถถอดรหัสได้:
- **`Hosts`** - ที่อยู่ IP ของ C&C Server
- **`Port`** - พอร์ตการเชื่อมต่อ
- **`KEY`** - คีย์สำหรับการเข้ารหัสข้อมูล
- **`SPL`** - Splitter/Delimiter สำหรับแยกข้อมูล
- **`Groub`** - Group identifier
- **`USBNM`** - USB device name
- **`InstallDir`** - ไดเรกทอรีการติดตั้ง
- **`InstallStr`** - สตริงการติดตั้ง
- **`Sleep`** - ระยะเวลา Sleep interval (ไม่เข้ารหัส)

## 🚀 การใช้งาน

### ความต้องการของระบบ
```bash
pip install pycryptodome
```

### วิธีการรัน
```bash
python aes.py
```

### ตัวอย่างผลลัพธ์
```
[Key Info]
  Mutex              : cXXJfXZBAucVLYeE
  MD5(Mutex)         : 71789d45c5a84ec7341744008a9169830
  Derived Key (hex)  : 71789d45c5a84ec7341744008a916983071789d45c5a84ec7341744008a91698300
  IV (hex)           : 00000000000000000000000000000000

=== Decrypted Settings ===
Hosts: 192.168.1.100  (IPv4)
Port: 8080  (Port)
KEY: MySecretKey123
SPL: |'|'|
Groub: DefaultGroup
USBNM: MyUSB
InstallDir: %APPDATA%\Windows
InstallStr: svchost.exe
Sleep: 3
```

## 🔬 รายละเอียดทางเทคนิค

### อัลกอริทึมการเข้ารหัส
- **Algorithm**: AES-256-CBC
- **Key Derivation**: MD5(Mutex) → 32-byte key with overlapping pattern
- **IV**: Zero-filled (16 bytes of 0x00)
- **Encoding**: Base64 → AES Decrypt → PKCS7 Unpad

### กระบวนการถอดรหัส
1. **Key Generation**: ใช้ Mutex string สร้าง MD5 hash
2. **Key Expansion**: สร้าง 32-byte key จาก 16-byte MD5
3. **Base64 Decode**: ถอดรหัส Base64 encoded ciphertext
4. **AES Decryption**: ใช้ AES-CBC mode ถอดรหัส
5. **Padding Removal**: ลบ PKCS7 padding หรือ null bytes
6. **Text Conversion**: แปลงเป็น readable text format

### โครงสร้างข้อมูล
```python
SETTINGS = {
    "Hosts":       "Base64_Encrypted_IP",
    "Port":        "Base64_Encrypted_Port", 
    "KEY":         "Base64_Encrypted_Key",
    "SPL":         "Base64_Encrypted_Splitter",
    "Sleep":       3,  # Plain integer (not encrypted)
    "Groub":       "Base64_Encrypted_Group",
    "USBNM":       "Base64_Encrypted_USB_Name",
    "InstallDir":  "Base64_Encrypted_Install_Directory",
    "InstallStr":  "Base64_Encrypted_Install_String",
    "Mutex":       "PlainText_Mutex_String",
}
```

## 📝 ตัวอย่างการใช้งานในโค้ด

### การถอดรหัสแบบ Manual
```python
from aes import derive_key_from_mutex, decrypt_b64_cbc, to_text

# สร้าง key จาก mutex
mutex = "cXXJfXZBAucVLYeE"
key = derive_key_from_mutex(mutex)
iv = b"\x00" * 16

# ถอดรหัสการตั้งค่า
encrypted_host = "imFZRbKTsm8HZs2ZNu1ccVAro2qZSCdi4MXKvpUUP3g="
decrypted_bytes = decrypt_b64_cbc(encrypted_host, key, iv)
host_ip = to_text(decrypted_bytes)
print(f"C&C Server: {host_ip}")
```

## ⚠️ คำเตือนและข้อควรระวัง

- **🎓 เพื่อการศึกษาเท่านั้น**: เครื่องมือนี้พัฒนาขึ้นเพื่อการศึกษาและวิจัยด้านความปลอดภัย
- **⚖️ ใช้อย่างถูกกฎหมาย**: ห้ามใช้เครื่องมือนี้เพื่อกิจกรรมที่ผิดกฎหมาย
- **🛡️ ความปลอดภัย**: ควรรันในสภาพแวดล้อมที่ปลอดภัย (Sandbox/VM)
- **🔒 ความรับผิดชอบ**: ผู้ใช้ต้องรับผิดชอบการใช้งานเครื่องมือนี้

## 📄 License

โปรเจคนี้เผยแพร่เพื่อการศึกษาและวิจัย กรุณาใช้อย่างรับผิดชอบและถูกกฎหมาย

---

**⚠️ Disclaimer**: เครื่องมือนี้พัฒนาขึ้นเพื่อการศึกษาและการวิจัยด้านความปลอดภัยเท่านั้น
